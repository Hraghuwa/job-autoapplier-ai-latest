# ── Copy this file to backend/.env and fill in your values ──────────────────
# NEVER commit backend/.env to git

# Database (Docker default)
DATABASE_URL=postgresql+asyncpg://jobagent:jobagent@localhost/jobagent

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT — generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_ME_32_BYTE_HEX

# Fernet — generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=CHANGE_ME_FERNET_KEY

# Gemini (your system key — from existing config.py)
SYSTEM_GEMINI_KEY=AIzaSyAfN5DAZhXhEkSDHEs4JzmWHi5QwLnXtz8

# Razorpay (leave blank until Step 3e)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# Resend email (leave blank until Step 5a)
RESEND_API_KEY=

# App
FRONTEND_URL=http://localhost:3000
ADMIN_EMAIL=your_admin_email@example.com
