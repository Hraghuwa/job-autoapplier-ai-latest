# ── Agent / Browser deps (local machine only — NOT installed on Railway) ──────
# Run locally: pip install -r requirements-agents.txt
-r requirements.txt
selenium==4.18.1
webdriver-manager==4.0.1
undetected-chromedriver>=3.5.5
